<%@ page import="com.ccit.dao.CardDAO"%>

<%
	CardDAO cd = new CardDAO();
	String id = request.getParameter("id");
	if (cd.find(code) != null) {
		request.setAttribute("user", cd.find(id));
		request.getRequestDispatcher("/cardupdate.jsp").forward(
				request, response);
	}
%>