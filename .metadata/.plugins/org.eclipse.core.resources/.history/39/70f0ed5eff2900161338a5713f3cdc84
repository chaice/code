package com.ccit.dao;

import java.sql.CallableStatement;
import java.sql.Connection;
import java.sql.DriverManager;
import java.sql.SQLException;

import com.ccit.entity.Entity;

public class Service {
	private String mes = "";
	public String bo_bor(String bd , String cd) {
        String sql = "call pro_bro(?,?,?)";
        borrDAO(sql);
        mes = cs
		return null;
	}

	public String bo_ret() {

		return null;
	}

	private CallableStatement borrDAO(String sql) {
		try {
			Connection con = DriverManager.getConnection(Entity.URL);
			CallableStatement cs = con.prepareCall(sql);
		} catch (SQLException e) {
			e.printStackTrace();
		}
		return cs;
	}
}
